🔐 LOGIN & VERIFICATION - COMPLETE ANALYSIS
==========================================

✅ FRONTEND API CALL IS PERFECT!
--------------------------------

Frontend (LoginPage.tsx) sends:
{
  "email": "user@example.com",
  "password": "password123",
  "useCookie": true
}

Backend (authController.ts) expects:
{
  "email": "...",
  "password": "..."
}

✅ The API call format is CORRECT!


🎯 WHY LOGIN IS FAILING - POSSIBLE REASONS
-------------------------------------------

1. ❌ USER NOT VERIFIED
   - You MUST verify email first before login
   - Backend blocks unverified users with: "Please verify your email before logging in"
   
2. ❌ VERIFICATION CODE NOT WORKING
   - Frontend sends: { email, otp }
   - Backend now accepts BOTH: { code } OR { otp }
   - ✅ THIS WAS FIXED!

3. ❌ OLD USER WITH DOUBLE-HASHED PASSWORD
   - If you created user BEFORE our fix, password is double-hashed
   - Login will always fail for these old users
   - Solution: Delete old test users and create new ones

4. ⚠️ API URL MISMATCH
   - Frontend calls: https://api.kynajewels.com/api/auth/login
   - Are you testing LOCALLY? Should be: http://localhost:5000/api/auth/login
   - If testing locally, update the API URL!


🔧 HOW TO TEST PROPERLY
-----------------------

STEP 1: Check User Status
   Run: node server/fix-old-users.js
   This shows all users and their verification status

STEP 2: Delete Old Test Users (if any exist from before fix)
   Option A: MongoDB Compass - manually delete
   Option B: Run in mongo shell:
      db.users.deleteMany({ email: "test@example.com" })

STEP 3: Restart Server with Logs
   The server now prints detailed logs:
   
   SIGNUP:
   ✅ User created successfully
   📧 Email: user@example.com
   🔑 Verification code: 522652
   ⏰ Code expires at: [timestamp]
   
   VERIFICATION:
   🔍 =============== EMAIL VERIFICATION ===============
   📥 Request body: { email: "...", otp: "522652" }
   📥 Code/OTP received: 522652
   ...
   ✅ STEP 4: All checks passed - Verifying user
   =============== VERIFICATION SUCCESS ===============
   
   LOGIN:
   🔐 =============== LOGIN ATTEMPT ===============
   📧 Email: user@example.com
   ...
   📋 STEP 1: User lookup
     User found: ✅ YES
   ✓ STEP 2: Verification check
     ✅ User is verified
   🔑 STEP 3: Password verification
     Password valid: ✅ YES
   =============== LOGIN SUCCESS ===============

STEP 4: Test Flow
   1. Go to Signup page
   2. Enter: name, email, password
   3. Check server console for verification code
   4. Enter the 6-digit code
   5. After verification success, go to Login page
   6. Enter same email + password
   7. Should login successfully!


📊 WHAT TO CHECK IN SERVER CONSOLE
-----------------------------------

If VERIFICATION fails, console will show:
   ❌ Invalid verification code - no matching user
   OR
   ❌ Token has expired!
   OR
   ⚠️ STEP 2: User already verified

If LOGIN fails, console will show:
   ❌ User not found
   OR
   ❌ User not verified
   OR
   ❌ Invalid password  <-- This means password doesn't match!


🐛 DEBUGGING COMMANDS
---------------------

1. Check all users:
   node server/fix-old-users.js

2. Check specific user in MongoDB:
   use kynajewels
   db.users.find({ email: "user@example.com" })

3. Check password hash:
   db.users.findOne({ email: "user@example.com" }, { password: 1, isVerified: 1 })

4. Delete specific test user:
   db.users.deleteOne({ email: "test@example.com" })

5. Delete ALL unverified users:
   db.users.deleteMany({ isVerified: false })


✅ NEXT STEPS
-------------

1. Run: node server/fix-old-users.js
2. Share the output with me
3. Restart server and check logs during login attempt
4. Share what the server console prints

This will help me see EXACTLY where it's failing!

